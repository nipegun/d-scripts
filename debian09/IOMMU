# dmesg | grep sd

# GRUB_CMDLINE_LINUX="iommu=soft"

# intel_iommu=on

# GRUB_CMDLINE_LINUX_DEFAULT="quiet intel_iommu=on pcie_acs_override=downstream i915.enable_hd_vgaarb=1"

# my only advice is stay away from Marvell RAID/SSD controllers at all cost if you ever plan to use IOMMU!
